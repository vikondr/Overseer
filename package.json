{
  "name": "overseer",
  "private": true,
  "scripts": {
    "services": "docker compose up -d azurite postgres backend pixeldiff",
    "services:all": "docker compose up -d",
    "services:down": "docker compose down",

    "desktop": "npm run services && cd overseer-desktop && npm start",
    "web": "npm run services:all && cd overseer-frontend && npm run dev",

    "desktop:only": "cd overseer-desktop && npm start",
    "web:only": "cd overseer-frontend && npm run dev"
  }
}